/**
 * Development environment configuration.
 * Update `apiUrl` when pointing to a real OpenAI-compatible backend.
 */
export const environment = {
  production: false,
  apiUrl: '/api',
  // apiUrl: 'http://localhost:3001/api'
};
